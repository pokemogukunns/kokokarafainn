services:
  - type: web
    name: teipenn2
    env: python
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt 
    startCommand: python youtube2.py
    autoDeploy: true
